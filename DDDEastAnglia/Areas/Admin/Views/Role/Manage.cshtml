@using DDDEastAnglia.Areas.Admin.Models
@model DDDEastAnglia.Areas.Admin.Models.RoleModel

@section header
{
    @Styles.Render("~/Content/admin")
}

<h2>Manage</h2>

@using (Html.BeginForm("Manage", "Role"))
{
    @Html.HiddenFor(r => Model.RoleName)
    <div>
        @Model.RoleName
    </div>
    <table>
        @for (int i = 0; i < Model.roleUsers.Count; i++)
        {
            <tr>
                <td>
                    @Html.HiddenFor(m => Model.roleUsers[i].UserId)@Html.HiddenFor(m => Model.roleUsers[i].Username)
                    @Model.roleUsers[i].Username
                </td>
                <td>@Html.CheckBoxFor(m => Model.roleUsers[i].IsMember)</td>
            </tr>
  
        }

        @foreach (RoleUserModel roleUser in Model.roleUsers)
        {
        }
    </table>
    <input type="submit" value="Save"/>
}
<a href="@Url.Action("Index", "Role")">Back to list</a>