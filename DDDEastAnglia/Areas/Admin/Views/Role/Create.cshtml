@model string

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@if (ViewBag.Message != null)
{
    <div>
        @ViewBag.Message
    </div>
}

@using (Html.BeginForm("Create", "Role"))
{
    <div>
    
        New Role: @Html.TextBox("role",Model)
    </div>
    <div>
        <input type="submit" value="Create"/>
    </div>
}
<div>
    <a href="@Url.Action("Index", "Role")">Return to list</a>
</div>