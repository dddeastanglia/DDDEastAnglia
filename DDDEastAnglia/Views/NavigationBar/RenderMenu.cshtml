@model DDDEastAnglia.Models.NavigationMenuViewModel

<ul class="nav nav-pills">
    @foreach (var link in Model.Links)
    {
        if (link.IsVisible)
        {
            <li class="dropdown @(link.IsActive ? "active" : "")"><a href="@link.LinkUrl">@link.LinkText</a></li>
        }
    }
</ul>
