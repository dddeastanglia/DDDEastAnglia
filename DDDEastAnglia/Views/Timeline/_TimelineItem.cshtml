@model DDDEastAnglia.Models.TimelineItemModel

<dt class="@(Model.PeriodPassed ? "passedTimelinePeriod" : "")">@ViewData["Label"]</dt>
<dd class="@(Model.PeriodPassed ? "passedTimelinePeriod" : "")">@Model.PeriodDate</dd>

